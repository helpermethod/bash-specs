describe "up"

before_each() {
  dir=$PWD
  mkdir -p .sandbox/foo/bar
  cd .sandbox
}

after_each() {
  cd "$dir"
  rm -r .sandbox
}

it "should move to parent directory" '{
	cd foo/bar
  up
  [[ $PWD = "$dir/.sandbox/foo" ]]
}'
